
*.apk
*.apkm
*.apks
# a stale one committed here is actively misleading.
# at the repo root, where provision.sh reads it. All of it is reproducible from
# Build output. build.sh writes artifacts under the paths below and the manifest
# checkout. Local state, never shared.
# dead: references $ROM/*.img paths that never existed
downloads/
# Droidian build: sources and artifacts are fetched/generated, only the
droidian-build-tools/
# droidian-camera fork: source is cloned and patched, artifacts generated
droidian/camera-src/
droidian/kernel/
droidian/linuxroot.img
droidian/linuxroot.simg
droidian/linuxroot.simg.part
droidian/local-repo/
droidian/out/
droidian/out-adaptation/
droidian/out-camera/
droidian/rootfs/
droidian/stage/
edl/
.edl-acknowledged
edl_config.json
edl-fix-critical.sh
# edl runtime cache (contains the device serial)
# Everything bootstrap.py fetches or produces. Nothing here belongs in git:
# fajita packaging overlay and the build script are tracked
.idea/
lineage/out/
# Local scratch
logs/
/manifest.json
memory/
msm/
oppo_decrypt/
out/
*postmarketOS*.img
*postmarketOS*.img.xz
*.pyc
__pycache__/
# Ramdumps pulled by droidian/dump-ramoops.py -- device RAM, regenerable
# Records that the operator accepted the destructive EDL warning on this
releases/
# the archives are ~6 GB and the extracted images are another ~5 GB.
# the source commit the manifest records, so none of it is worth versioning and
# Unrelated postmarketOS images that happen to live in this directory
.venv/
.work/
*.xapk
